#include <stdio.h>

int main() {
    int i=0,n,a, s=0;
    scanf("%d %d",&a,&n);
    while(n<=0) {
        scanf("%d", &n);
    }
    for(i=a;i>0;i--){
        s += i;
    }
    for(i=n-1;i>0;i--){
        s += i;
    }
    printf("%d\n", s);
    return 0;
}
